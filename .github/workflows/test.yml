name: github actions demo

on:
  push:
    branches: [ bazel_demo ]

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
    - uses: |
        actions/checkout@v2
        ngalaiko/bazel-action/2.0.0@master
    - name: bazel build test
      run: |
        echo "hello"
        
    
